main_menu:
    - { title: Home, url: "{{ '/' | relative_url }}" }
    - { title: About, url: "{{ '/about/' | relative_url }}" }
    - { title: Visualizations, url: "{{ '/visualizations/' | relative_url }}" }
